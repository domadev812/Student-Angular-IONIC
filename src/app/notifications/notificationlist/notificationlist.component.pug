#notificationslist
  ngx-loading([show]='loading') 
  .d-flex.space-between.px-4.py-1
    .table-header.col-subject
      | SUBJECT
    .table-header.col-message
      | MESSAGE
    .table-header.col-sent
      | SENT
    .table-header.col-view
      | &nbsp;
  .table-content-without-search(#scrollVariable appInfiniteScroller [scrollPercent]="90" [scrollCallback]="myScrollCallBack.bind(this)")
    .list(*ngFor="let notification of notifications" (click)='viewNotification(notification.id)')
      .d-flex.space-between.bg-white.px-4.py-1
        .table-data-primary.col-subject
          | {{notification.subject}}
        .table-data.col-message
          | {{notification.body}}
        .table-data.col-sent
          | {{notification.created_at}}
        .table-data.col-view
          i.fa.fa-eye.edit-row
    .infinite-scroll-loading(class="infinite-scroll-loading" *ngIf="infiniteScrollLoading")
      | Loading ...